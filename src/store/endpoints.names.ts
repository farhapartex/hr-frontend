import { API_HOST } from "@/global";
const API_URL: string = `${API_HOST}/api/v1`;
export const LOGIN_ENDPOINT: string = `${API_HOST}/rest-auth/login/`;
export const LOGOUT_ENDPOINT: string = `${API_HOST}/rest-auth/logout/`;